// npm - global command, comes with node
// npm --version

// local dependency - use it only in this particular project
// npm i <packageName>

// global dependency - use it in any project (npx makes this more or less redundant)
// npm install -g <packageName>

//package.json - manist file (stores important uinfo about project/package)
// manual approach (create package.json in the rootm, create properties)
// npm init (step by step)
// npm init -y (automatic)
